# Story 1.1: Project Setup

**Status:** Ready for Review

**As a** developer,
**I want** to set up the initial monorepo with separate packages for the frontend (React) and backend (Spring Boot),
**so that** we have a clean and organized codebase from the start.

## Acceptance Criteria

1.  A Git repository is created.
2.  The repository contains a `frontend` directory with a new React application.
3.  The repository contains a `backend` directory with a new Spring Boot application.
4.  A root `package.json` or build script can run both applications.

## Dev Notes

### Previous Story Insights

No previous story.

### Data Models

No specific data models are directly involved in project setup.

### API Specifications

No endpoints yet; REST structure will be defined in subsequent stories.

### Component Specifications

No specific UI components are directly involved in project setup. For building the application's UI components we will use Radix UI (unopinionated, accessible primitives) together with our design system.

### File Locations

- `taskflow/apps/web/` for React application. [Source: architecture/unified-project-structure.md]
- `taskflow/apps/api/` for Spring Boot application. [Source: architecture/unified-project-structure.md]
- `taskflow/packages/shared/` for shared types. [Source: architecture/unified-project-structure.md]

### Testing Requirements

- Verify that both applications compile and start successfully.

- Add placeholder JUnit test class in backend (src/test/java/com/taskflow/ApplicationTests.java).

- Add placeholder Jest or Vitest test in frontend (src/**tests**/App.test.tsx).

### Technical Constraints

- Frontend: React.js 18.x, TypeScript 5.x [Source: architecture/tech-stack.md]
- Backend: Java 21, Spring Boot 3.x [Source: architecture/tech-stack.md]
- Repository: Monorepo with npm workspaces [Source: architecture/unified-project-structure.md]

## Tasks / Subtasks

- [x] **Initialize Git Repository:** Create a new Git repository for the project.
- [x] **Create Monorepo Structure:** Set up the `taskflow/` directory with `apps/`, `packages/`, and a root `package.json` for npm workspaces. [Source: architecture/unified-project-structure.md]
- [x] **Create React Frontend Application:**

* Inside apps/web/, create React app with TypeScript template
* Add ESLint + Prettier configuration
* Confirm npm start runs successfully

- [x] **Create Spring Boot Backend Application:**

* Inside apps/api/, initialize Spring Boot 3.x project using Spring Initializr
* Include dependencies: Spring Web, Spring Data JPA, MySQL Driver, Spring Boot DevTools, and Lombok
* Add base package: com.taskflow
* Confirm backend runs on port 8080

- [x] **Configure Root `package.json`:** Add scripts to the root `package.json` to run both frontend and backend applications. (AC: 4)
- [x] **Create Shared Package:** Create `packages/shared/` for shared TypeScript interfaces.
- [x] **Setup JUnit & Jest Tests:**

* Backend: Add ApplicationTests.java annotated with @SpringBootTest.
* Frontend: Add App.test.tsx with a basic render test.

- [x] **Configure CI Pipeline:**

* Add .github/workflows/build.yml to verify both apps build successfully.
* Steps: checkout → install dependencies → build frontend → build backend.

## Dev Agent Record

### File List

- `package.json`
- `apps/web/` (and all its content)
- `apps/api/` (and all its content)
- `packages/shared/`
- `.github/workflows/build.yml`
